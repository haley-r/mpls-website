import React from 'react';

//turn this into a class component, lol

const AdminDashboard = () => (
    <p>this is where the admin dashboard will go. u can only see if logged in!</p>
);

export default AdminDashboard;
